<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of users</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= data.username %>'s Pantries</li></h1>
    <% if (data.pantry.length > 0) { %>
        <ul>
            <% data.pantry.forEach((el)=>{ %>
            <li><%= el.name %></li>
            </a>
        <% }) %>
        </ul>
    <% } else { %>
        <h3>This user has no pantries yet</h3>
    <% } %>
  </body>
</html>